extends Node
class_name GlobalSignalBus

signal multi_ball_requested()
signal powerup_spawn_requested( Vector2 )
signal all_balls_escaped()
signal lives_updated( int )
signal ball_escaped()
signal brick_broken( int )
